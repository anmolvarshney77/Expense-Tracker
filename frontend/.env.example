# Backend API base URL (no trailing slash). Backend must allow this frontend origin in CORS.
# Local (use 127.0.0.1 to avoid IPv6 issues):
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
# Production: your deployed backend URL.
