# Django
SECRET_KEY=your-secret-key-change-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# PostgreSQL (use DATABASE_URL or individual vars)
DATABASE_URL=postgresql://user:password@localhost:5432/expenses_db
# Or:
# DB_NAME=expenses_db
# DB_USER=user
# DB_PASSWORD=password
# DB_HOST=localhost
# DB_PORT=5432

# CORS - frontend origin(s), comma-separated (include port if not 3000, e.g. 3001)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
# Optional: set to True to use SQLite for local dev (no PostgreSQL required)
# USE_SQLITE=True
